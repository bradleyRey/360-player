// File for the main functions for the server

var $window = $(window);
var $username = $(.username);
var $login = $(.login);

//prompt username

var input;
var userconnected = false;

//bringing in the socket.io object
var socket = io();

// prompting username

function(input){

}
